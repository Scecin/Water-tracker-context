// Import createContext to can use context
import { createContext } from "react";

// Crete the variable twhere a call the method createContext
const UserContext = createContext()

// Export the file to can use it
export default UserContext